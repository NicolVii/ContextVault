/**
 * Starting wallet for new users — small bootstrap only.
 * Free monthly Auto is tracked in plan_usage_periods, not this grant.
 */
export const DEFAULT_SIGNUP_CREDITS = 3_000;

/** Yardstick for support/details estimates (not primary customer UX). */
export const STANDARD_CONVERSATION_CREDITS = 100;
